import React from 'react';
import { SiteContent } from '@components/layout';

export default function Typography() {
    return (
        <SiteContent title="Typography">
        </SiteContent>
    );
}
