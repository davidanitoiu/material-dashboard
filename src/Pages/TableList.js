import React from 'react';
import { SiteContent } from '@components/layout';

export default function TableList() {
    return (
        <SiteContent title="TableList">
        </SiteContent>
    );
}
